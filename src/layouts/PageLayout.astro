---
import BaseLayout from './BaseLayout.astro';
import DocsMenu from '../components/DocsMenu.astro';
const { pageTitle, url } = Astro.props;
---

<BaseLayout pageTitle={pageTitle} url={url}>
  <aside class="sidebar" id="sidebar">
    <nav class="sticky" aria-label="articles">
      <DocsMenu />
    </nav>
  </aside>
  <article class="flow | prose | content | page" id="content">
    <h1>{pageTitle}</h1>
    <slot />
  </article>
</BaseLayout>
